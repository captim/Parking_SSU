<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <application
        android:name=".stat.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".others.SplashActivity"
            android:label="@string/splash"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".owners.OwnerListActivity"
            android:label="@string/owner_list"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".bind.BindActivity"
            android:label="@string/owner_bind_car"
            android:screenOrientation="fullSensor" />
        <activity android:name=".cars.CarAddActivity"
            android:label="@string/Bind_car"
            android:theme="@style/Theme.AppCompat.Light.Dialog"/>
        <activity android:name=".cars.CarListActivity"
            android:label="@string/car_list"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>